{
  "name": "stamp-rally",
  "version": "0.1.0",
  "private": true,
  "description": "A digital event stamp card — collect a stamp per stall, no install, no account.",
  "scripts": {
    "start": "serve . -l 3000",
    "test": "node --test \"tests/*.test.js\"",
    "test:watch": "node --test --watch \"tests/*.test.js\""
  },
  "license": "UNLICENSED",
  "dependencies": {
    "serve": "^14.2.6"
  }
}
